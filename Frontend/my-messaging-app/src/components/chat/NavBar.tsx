import { HeaderContainer } from "../common/HeaderContainer";

export const NavBar: () => JSX.Element = () => {
  return (
    <HeaderContainer>
      {/* Here the Chat Headers */}
      <></>
    </HeaderContainer>
  );
};
